# Core (required) — all Colab-preinstalled, lightweight, CPU-only
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3
matplotlib>=3.7

# Real data (default). Pulls the Kaggle credit-card-transactions dataset (needs a Kaggle token).
kagglehub>=0.2

# Optional — HuggingFace real datasets (gated set; needs `huggingface-cli login`).
# datasets>=2.16      # HuggingFace: mitulshah/transaction-categorization (4.5M rows)
